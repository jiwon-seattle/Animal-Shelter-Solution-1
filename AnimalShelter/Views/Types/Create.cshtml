@{
    Layout = "_Layout";
}

@model AnimalShelter.Models.Types

<h3>Add a new types</h3>
@using (Html.BeginForm())
{
    <h3>@Html.LabelFor(model => model.Name)</h3>
    @Html.TextBoxFor(model => model.Name)
    <br><br>
    <input class="btn btn-primary" type="submit" value="Add a new type" />
}

<hr>
<p class="link">@Html.ActionLink("Show all types", "Index")</p>
<p class="link">@Html.ActionLink("Home", "Index", "Home")</p>